"""patchpilot CLI — orchestrates triage → repair → verify → audit.

Commands:
    repair      Run local repair against a failing command
    repair-gh   Repair a failed GitHub Actions run
    diagnose    Classify a failure without modifying files
    init        Create .patchpilot.yml from defaults
    doctor      Validate environment setup
    runs        View run history (list / view <id>)
"""
